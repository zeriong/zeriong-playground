[project]
name = "analyzer"
version = "0.0.0"
description = "Python FastAPI service mounted at /analyze"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[tool.uv]
dev-dependencies = []
